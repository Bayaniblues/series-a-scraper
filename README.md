# Series A scraper

Used to scrape venture beat. 

Install requirements

    pip install -r requirements.txt

Then scrape

    scrapy crawl vbeat

At this point the scraper will only print out the return

Still under development